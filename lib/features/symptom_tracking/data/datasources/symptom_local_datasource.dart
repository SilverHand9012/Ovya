/// Local data source for symptom tracking.
/// Currently handled via Isar internally in the repository layer.
/// This class exists to preserve clean architecture boundaries.

class SymptomLocalDataSource {
  // Future: direct Isar access abstraction if needed
}
